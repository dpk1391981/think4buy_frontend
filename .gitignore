node_modules/
.next/
# Scratch build dir for `NEXT_DIST_DIR=.next-verify npm run build` — lets a
# production build run while `next dev` holds .next (see next.config.js).
.next-verify/
.env
dist/
build/